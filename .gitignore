.env
__pycache__/
venv/
processed_issues.json
node_modules/
.DS_Store
*.pyc
ab.log
# Runtime log files written by the service in its working dir; never commit these.
server.log
server_debug.log
server*.log
# config.json holds runtime/personal values (monitored repos, tokens, etc.) and
# must not be committed. Fresh installs seed from config.json.example instead.
config.json
